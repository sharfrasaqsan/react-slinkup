import CreatePost from "../components/CreatePost";
import Feed from "../components/feed/Feed";

const Home = () => {
  return (
    <section>
      <CreatePost />
      <Feed />
    </section>
  );
};

export default Home;
